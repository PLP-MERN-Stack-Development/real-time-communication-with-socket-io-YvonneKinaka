# Chat App (Socket.IO + Node + React + TypeScript)

Server:
- cd server
- npm install
- npm run dev

Client:
- cd client
- npm install
- npm run dev

Server runs on http://localhost:3001
Client runs on http://localhost:3000